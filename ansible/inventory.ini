[nextgame]
# Atualizado automaticamente pelo assistente com o IP público criado pelo Terraform
# Para usar, execute o playbook a partir de um host de controle (WSL/VM Linux recomendado):
#   ansible-playbook -i ansible/inventory.ini ansible/playbook.yml --private-key ~/.ssh/id_rsa
# Linha abaixo aponta para a instância criada (SSH + chave privada local)
98.93.15.51 ansible_user=ec2-user ansible_ssh_private_key_file=~/.ssh/id_rsa
